# Server environment example

# MongoDB connection string (e.g., MongoDB Atlas)
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-host>/idmrs?retryWrites=true&w=majority

# Express server port
PORT=5000

# Environment mode
NODE_ENV=development


